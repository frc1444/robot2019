Put jars in this folder that you want to access. Make sure to add them to the build.gradle in dependencies by adding:

compile name: '<YOUR JAR NAME>'

However, it is preferable to use the gradle configuration as much as possible.